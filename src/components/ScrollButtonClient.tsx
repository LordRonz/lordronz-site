'use client';

import ClientWrapper from './ClientWrapper';
import ScrollButton from './ScrollButton';

const ScrollButtonClient = () => {
  return (
    <ClientWrapper>
      <ScrollButton />
    </ClientWrapper>
  );
};

export default ScrollButtonClient;
