# Implementation Plans

Generated by the improve skill on 2026-08-15 against commit `deebe5b`. Execute
in the order below unless dependencies say otherwise. Each executor must read
its plan fully, honor STOP conditions, and update its status row when done.

## Execution order & status

| Plan | Audit finding | Title                                | Priority | Effort | Depends on            | Status |
| ---- | ------------: | ------------------------------------ | -------- | ------ | --------------------- | ------ |
| 001  |             5 | Show only existing blog translations | P1       | S      | Verification baseline | DONE   |
| 002  |             6 | Handle Spotify HTTP errors           | P1       | S      | Verification baseline | DONE   |
| 003  |            12 | Lazy-load the project lightbox       | P2       | S      | Verification baseline | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- All three plans require a working Vitest and ESLint baseline. At planning
  time, `bun run test` failed before collecting tests because
  `@vitejs/plugin-react` requires Vite 8 while `bun.lock` resolves Vite 7, and
  `bun run lint` failed because the lockfile mixes ESLint 10 with lint packages
  pinned to ESLint 9. Restoring those gates was audit finding 1 but was not
  selected for planning. Executors must STOP rather than edit dependencies.
- Plans 001 and 002 are otherwise independent and may run in either order after
  the baseline is restored.
- Plan 003 is independent of the blog/API fixes and is P2 because it is a
  payload optimization rather than a user-visible correctness bug.
- Each plan touches a separate source area, so no selected plan depends on
  another selected plan.

## Findings considered and rejected

- Cache the complete MDX/frontmatter pipeline: rejected because the
  recommendation result is not rendered; deleting that unused work is smaller,
  and the remaining 15-post build does not justify a cache.
- Add a new all-in-one local check abstraction: rejected because the repository
  already has exact verification commands and CI jobs; repairing them is enough.
- Promote all 134 dependency-audit entries into plans: rejected because many are
  dev-only or unreachable transitive paths. Only evidence-backed reachable
  findings should be planned.
